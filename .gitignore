# deps / build
node_modules
.next
out
dist
next-env.d.ts
*.tsbuildinfo

# env
.env
.env.local

# python sidecar
tts-server/.venv
tts-server/__pycache__
tts-server/**/__pycache__

# cloned voices (generated at runtime) — keep the dir, ignore contents
tts-server/voices/*
!tts-server/voices/.gitkeep
